import { h, Component } from 'preact';

import './About.scss';

export default class About extends Component {
  /**
   * Render
   */
  render() {
    return (
      <div>
        About
      </div>
    );
  }
}